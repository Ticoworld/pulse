{
  "name": "@pulse/executor",
  "version": "0.1.0",
  "private": true,
  "description": "Pulse Execution Bot (Phase 8)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npx ts-node src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@pulse/db": "*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2"
  }
}
